{
  // Manual long-term overlay for curated ticker biases.
  // Update this file weekly if desired. Only include keys supported by the
  // policy schema; unknown keys may cause validation errors.
  //
  // IMPORTANT:
  // - ticker_bias values are clamped to [-0.20, 0.20].
  // - If you do NOT want MA-break EXIT→TRIM downgrades from bias alone,
  //   keep values below 0.05 (see thresholds.tilt_exit_downgrade_min).
  // - Do NOT edit config/policy_overrides.json directly.
  "ticker_bias": {}
}

