{
  "name": "broker-gpt",
  "version": "0.0.0",
  "private": true,
  "description": "Broker GPT tooling and automation",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "node scripts/postinstall-codex-global.js"
  },
  "devDependencies": {
    "@openai/codex": "latest"
  }
}
